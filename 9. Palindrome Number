9. Palindrome Number


class Solution {
    public boolean isPalindrome(int x) {
  if(x<0)return false;
     int s=x;
     int d=0;
     while(x>0)
     {
           d=d*10+x%10;
           System.out.println(d);
           x/=10;

     }
  return d==s;
    }
}
